# Nexus

A lightweight AI agent framework with a separable core, agent implementations, and simple tests.

## Structure

- `nexus/core/`: shared engine components
- `nexus/agents/`: agent implementations
- `tests/`: test coverage for core behavior

## Quick start

1. Create a virtual environment.
2. Install dependencies from `requirements.txt`.
3. Run tests with `pytest`.
