[project]
name = "nexus"
version = "0.1.0"
description = "A modular Nexus AI agent framework."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
